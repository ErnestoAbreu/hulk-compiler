type Point(x : Number,y : Number) {
    x = x;
    y = y;

    getX() => self.x;
    getY() => self.getX() + self.y;

    setX(x) => self.x := x;
    setY(y) => self.y := y;
}

type PolarPoint inherits Point {
    rho() => 5;
}

let p  = new PolarPoint(4,5) in {
    print(p.getX() @@ p.getY());
};