type Point {
    x = 3;
    y = 4;

    getX(n) => self.x + n;
    getY() => self.y;

    setX(x) => self.x := x;
}

let a = new Point() in {
    print(a.getX(10));
    print(a.setX(10));
    print(a.getX(10));
}
